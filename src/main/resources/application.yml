spring:
  application:
    name: AppProdutos

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:h2:mem:produtosdb
    username: sa
    password: